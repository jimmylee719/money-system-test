# dependencies
node_modules/

# build output
dist/
.next/
out/

# L0 原始快照（體積大且每日成長，不進版控；正式環境走 Supabase / R2）
data/

# secrets — 絕不可提交（CLAUDE.md 禁止事項）
.env
.env.*
!.env.example
*.pem
*.p12
service-role*.json

# Supabase CLI 產物（linked-project.json 只有 ref/名稱，無金鑰，但仍不需進版控）
supabase/.temp/

# logs / cache
*.log
.vitest/
coverage/
.DS_Store
Thumbs.db

# Excel 匯出檔（可隨時重新產生，不進版控）
exports/

# Next.js
.next/
next-env.d.ts
*.tsbuildinfo
