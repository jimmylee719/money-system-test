# Claude Code 啟動 Prompt

> 用法：先把 `CLAUDE.md` 放進專案根目錄，再把下面對應的 prompt 整段貼給 Claude Code。
> 每個 Phase 通過驗證後，用「通用 Prompt」填入下一階段推進。

---

## 【啟動 Prompt — P0】

```
請先讀取專案根目錄的 CLAUDE.md，並套用 genesis-protocol skill。
全程繁體中文回覆，系統面（欄位名、變數、API、schema）用英文。

## 專案背景
台股波段分析與訊號系統。v1 只產生訊號、LINE 通知與人工紀錄，不下單。
目標不是預測股價，是紀律執行、成本精算、風險控制。

## 本次任務：P0 — 成本與損益兩平計算模組

這是系統的第一個模組，獨立於券商與策略，是後續所有決策的基礎設施。

### 功能需求
輸入：
- 進場價、股數、交易類型（一般 / 現股當沖）
- 券商手續費折讓率、最低手續費金額
輸出：
- 買進手續費、賣出手續費、證券交易稅
- 損益兩平價（breakeven price）
- 指定停損價／停利價下的「淨」損益與實際 R 倍數

### 費率規則（請先自行查證確認現況，不要直接採信以下數字）
- 手續費 0.1425%，買賣各收一次，適用券商折讓，且不低於券商最低手續費
- 證交稅：一般交易 3‰（賣出時收取）
- 現股當沖證交稅減半 1.5‰，依《證券交易稅條例》第 2 條之 2，
  施行期限延長至 2027-12-31
- ⚠️ 本系統不做當沖，但計算器仍須支援該模式供比較用

### 技術要求
- TypeScript strict，純函式，無副作用，不依賴任何外部服務
- 金額計算不得用浮點數直接運算（請選定處理方式並說明理由）
- 完整型別定義，所有輸入輸出皆有 interface
- 附 Vitest 測試

### 驗證閘門（未通過不得 commit）
提供至少 3 組手算案例（含小額觸發最低手續費的邊界情況），
實際執行測試，證明程式輸出與手算完全一致。

### 交付格式
1. 先給我**檔案結構與函式簽章**，等我確認後再寫實作
2. 實作完成後實際執行測試並貼出結果
3. 通過後才 commit，格式 `P0: 成本與損益兩平模組`，回報 commit hash
4. 最後說明：驗證了什麼、怎麼驗的、還有什麼沒驗證

### 禁止
- 不要一次傾倒全部程式碼
- 不要跳過驗證直接說完成
- 費率數字若查不到官方來源，明確說「查無」，不要憑記憶填入
```

---

## 【通用 Prompt — P1 之後每個階段】

```
請讀取 CLAUDE.md 並套用 genesis-protocol。繁體中文回覆。

## 本次任務：{Phase 編號} — {階段名稱}

### 前置狀態
上一階段：{Phase 編號}，commit：{hash}
已完成：{一句話}

### 本階段範圍
{只寫這一階段，不要跨階段}

### 驗證閘門
{必須可實際執行驗證的條件，寫死}

### 交付流程
1. 先給計畫與檔案結構，我確認後才動工
2. 實作 → 實際執行驗證 → 貼出結果
3. 通過才 commit，格式 `{Phase}: {階段名稱}`，回報 hash
4. 說明驗證了什麼、殘餘風險是什麼

### 提醒
- L0 只存不判斷；L2 只能否決不能觸發
- append-only 約束要實測（試著 UPDATE / DELETE 應被 RLS 擋下）
- 任何時間欄位不得用 now() 代替來源的 data_as_of
- daily_picks 與 user_records 必須分表
- 查不到的事實說查不到，不要生成
```

---

## 【各 Phase 驗證閘門對照】

| Phase | 階段 | 驗證閘門 |
|---|---|---|
| P0 | 成本／損益兩平模組 | 3 組手算與程式輸出一致（含最低手續費邊界） |
| P1 | L0 抓取 TWSE / TPEx | 連續 5 交易日零缺漏、零人工介入；休市日正確跳過 |
| P2 | L0 擴充 MOPS / TAIFEX | 上市＋上櫃覆蓋完整；月營收於申報期後正確入庫 |
| P3 | append-only + RLS + drift | 實測 UPDATE/DELETE 被擋；content_hash 偵測回溯修改；drift 觸發 degraded |
| P4 | factor_registry | 跑通登記→檢定→裁決；空白 economic_rationale 被拒；registered_at 早於 tested_at |
| P5 | L1 訊號引擎 + Top 5 排序 | 手算排序與程式輸出一致；時間欄位正確；每日穩定產出 5 檔 |
| P6 | L2 否決層（規則式） | 處置股、重訊利空、流動性不足能正確否決並記錄理由 |
| P7 | L3 風控 | 超額訊號被拒並記錄；部位計算與手算一致 |
| P8 | LINE 日報 + /rec 紀錄 | 收盤後 30 分內收到；Top 5 與訊號分區；紀錄正確落庫；無訊號日正確顯示 |
| P9 | outcomes 回填 + Excel 匯出 | T+5/10/20 正確計算；xlsx 六分頁可下載；outcomes 人工不可寫 |
| P10 | Dashboard + 0050 對照 | 能列出全部歷史推薦、紀錄、報酬、基準比較 |
| P11 | gold_set + 本機 LLM 佇列 | 離線時佇列累積不失敗；開機後補跑；llm_results 含 model_id + prompt_version |
| P12 | Meta-labeling + Purged CV + DSR | 不預測方向只預測成功機率；DSR 連同試驗次數呈報 |

---

## 【P8 補充規格：日報與紀錄指令】

貼 P8 時附上這段：

```
### 日報必須分兩區，不可混淆
- 觀察榜 Top 5：每日固定 5 檔，標示「研究紀錄，非買進建議」
- 交易訊號：通過 L2+L3 者，0～N 檔；若為 0 顯示「今日無符合風控條件之標的」

### 訊號六欄（缺一不可）
標的與現價／觸發原因（因子與數值）／建議進場價／停損價／停利價（≥2R）／
本筆最大虧損金額（新台幣絕對金額，不用百分比）

### 紀錄指令
/rec <代號> 觀望
/rec <代號> 買 <股數> <價格>
/rec <代號> 賣 <股數> <價格>
/rec <代號> 略過 <理由>
/note <代號> <文字>

### 防自我欺騙的四項約束（必須實作並實測）
1. daily_picks 於推播當下寫入 price_at_push，append-only
2. daily_picks 與 user_records 分表
3. outcomes 僅系統可寫，人工不可改
4. user_records 含 recorded_at，可偵測事後補登
```

---

## 【P9 補充規格：Excel 六分頁】

```
分頁與欄位：
每日推薦 | 日期、代號、名稱、排名、綜合分、各因子數值、price_at_push、是否通過 L2/L3
我的紀錄 | 日期、代號、動作、股數、價格、理由、備註、recorded_at
結果追蹤 | 每筆推薦的 T+5 / T+10 / T+20 報酬、是否觸及各屏障
績效彙總 | 期望值(R)、Profit Factor、勝率（僅供參考）、MDD、換手統計
0050對照 | 同期基準報酬與差額
因子表現 | 各因子 rolling 60 筆表現與衰減狀態

匯出檔存 Cloudflare R2，回傳有效期限的下載連結。使用 xlsx skill。
```

---

## 【給 Claude Code 的監督提醒】

遇到以下情況，請主動要求它停下來：

- 一次寫超過一個 Phase
- 說「應該可以」「大概是」而沒有實際執行驗證
- 填入沒有查證來源的費率、法規條號或 API 端點
- 把 LLM 或新聞判讀放進 L1（訊號層）
- 為了讓測試通過而修改測試條件
- 把「觀察榜 Top 5」寫成買進建議

任一發生，回覆：**「停。請說明你驗證了什麼、怎麼驗的。」**
